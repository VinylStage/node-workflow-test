name: 🎨 설계 / RFC
description: 아키텍처 설계나 기술적 의사결정을 논의합니다.
title: "[Design] "
labels: ["type: design"]
body:
  - type: markdown
    attributes:
      value: |
        설계 논의나 RFC(Request for Comments)를 작성해주세요.
        중요한 기술적 결정은 팀원들과 충분히 논의한 후 진행하는 것이 좋습니다.

  - type: textarea
    id: background
    attributes:
      label: 배경 및 문제 상황
      description: 왜 이 설계 논의가 필요한가요?
      placeholder: |
        예) 현재 로그 저장 방식이 대용량 데이터 처리에 부적합하여 새로운 아키텍처가 필요합니다.
    validations:
      required: true

  - type: textarea
    id: proposal
    attributes:
      label: 제안하는 설계
      description: 구체적인 설계안을 설명해주세요.
      placeholder: |
        예)
        - 현재: PostgreSQL 동기 처리
        - 제안: Kafka + Batch Processing
        - 이유: 초당 1000건 이상의 로그 처리 필요
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: 대안 방안
      description: 다른 접근 방법도 검토하셨나요?
      placeholder: |
        예)
        Option A: Redis Queue 사용 (장점: 간단, 단점: 데이터 유실 위험)
        Option B: RabbitMQ 사용 (장점: 안정성, 단점: 운영 복잡도)
    validations:
      required: true

  - type: textarea
    id: impact
    attributes:
      label: 영향 범위
      description: 이 설계 변경이 시스템에 어떤 영향을 미치나요?
      placeholder: |
        예)
        - 변경 필요 모듈: SecurityLogService, LogRepository
        - 데이터 마이그레이션 필요 여부: Yes
        - 기존 API 호환성: Maintained
    validations:
      required: true

  - type: textarea
    id: tradeoffs
    attributes:
      label: 트레이드오프
      description: 이 설계의 장단점을 명시해주세요.
      placeholder: |
        장점:
        - 처리 속도 10배 향상
        - 확장성 개선

        단점:
        - 시스템 복잡도 증가
        - 운영 비용 상승

  - type: textarea
    id: implementation
    attributes:
      label: 구현 계획 (선택사항)
      description: 단계별 구현 계획이 있다면 작성해주세요.
      placeholder: |
        Phase 1: 프로토타입 개발 및 검증
        Phase 2: 실제 환경 적용
        Phase 3: 기존 시스템 마이그레이션

  - type: textarea
    id: references
    attributes:
      label: 참고 자료
      description: 관련 문서, 논문, 벤치마크 결과 등을 첨부해주세요.
